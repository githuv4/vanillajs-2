const calculator = {
  plus: function (a, b) {
    return a + b;
  },
  minus: function (a, b) {
    return a - b;
  },
};

const result1 = calculator.plus(1, 3);
const result2 = calculator.minus(1, 3);
console.log(result2);
